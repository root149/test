- name: Test Zabbix Server  from Zabbix Client
    hosts: "{{ hosts }}"
    - tasks:
      vars:
       _zhost: "{{ zhost }}"
       _zport: "{{ zport }}"
       shell: nc -zv "{{ _zhost }}" "{{ _zport }}"

